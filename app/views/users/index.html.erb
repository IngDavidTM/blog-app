<% @users.each do |user| %>
  <div class="user-card">
    <img src="<%=user.photo%>" alt="<%=user.name%>'s photo"/>
    <div>
      <h2><%=user.name%></h2>
      <h3>Number of posts: <%=user.posts_counter%></h3>
    </div>
  </div>
<% end %>
